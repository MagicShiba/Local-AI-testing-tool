# 待办

增加题目、文件夹、测试集的重命名
增加删除题目、文件夹、测试集
增加设置页额外参数 JSON 校验提示


改善加载延迟,从f12查看当前加载信息，似乎后端会先尝试读取所有文件，改为只在需要时进行加载
详细分数(scoreStatsPopover)提升层级避免大小到受侧栏限制,并且支持在对比界面的"compare-result-header"内查看
对比界面支持查看详细分数,支持评分时排除某组,动态修改显示的分数

与测试本组对应的排除本组,一键测试时忽略

代码检查功能添加模板插入，模板放在app-data文件夹，支持在设置界面中编辑管理，先放下面两个检测就行。
function checkAnswer(answer, correctAnswer) {
    return answer === correctAnswer;
}

function checkAnswer(answer, correctAnswer) {
  if (answer == "C") { return true; //返回满分
  } else if (answer == "B") { 
    return { earned : 0.5 };  // 选 B 返回 50% 的分数
  } else { return false; }  //其它0分
}

页内小窗打开网页显示,例如测试题中的标记方框，检测函数中添加支持在对比界面显示的额外按钮,用于快速查看,例如测试题中的标记方框文件"00标注方框测试.json"
当前在测试页插入在test-side中，在对比页没有插入。

添加创建问题api接口,添加简洁的创建问题脚本，只填参数，然后调用api添加问题。并给出调用说明。放在app-data文件夹。

添加一个简单的聊天界面,支持文件夹分组,支持刷新回答(回答多次后在回答下面显示"< 2 >"格式的切换查看)，支持从某个对话创建分支(也就是创建一个新对话，保留上面对话内容), 支持插入图像, 支持对回答进行编辑, 支持导出成为问题

让message-bubble支持公式渲染

多results,公开results?
检查统计问题

# 已做
忽略文件夹上用于排序显示的数字
笔记使用真正markdown
分离设置中的api配置
页面切换移动至侧边栏 支持结果对比视图
区分分数,得分,总分,参与测试分数